name: Listen

on:
  deployment:
  deployment_status:

jobs:
  listen:
    name: listen
    runs-on: ubuntu-latest
    steps:
      - name: Dump context
        uses: crazy-max/ghaction-dump-context@v1
